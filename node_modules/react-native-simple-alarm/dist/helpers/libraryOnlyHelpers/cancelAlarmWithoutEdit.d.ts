/**
 * Cancel Alarm
 * Cancels push notfication for alarm without activation
 * @id {string || number}
 */
import { ID } from "../../Types";
export declare const cancelAlarmWithoutEdit: (id: ID) => Promise<void>;
