/**
 * Cancel Alarm
 * Cancels push notfication for alarm
 * @id {string || number}
 */
import { Alarm as AlarmType } from "../Types";
export declare const cancelAlarm: (alarm: AlarmType) => void;
export declare const cancelAlarmById: (id: string | number) => Promise<AlarmType>;
